import React, { type PropsWithChildren } from 'react';
import { DeckAdapter, FormatConfigs, Timecode } from '@hubble.gl/core';
type BasicControlsProps = {
    adapter: DeckAdapter;
    busy: boolean;
    setBusy: (busy: boolean) => void;
    formatConfigs: Partial<FormatConfigs>;
    timecode: Timecode;
    embed?: boolean;
    filename?: string;
};
export default function BasicControls({ children, adapter, busy, setBusy, formatConfigs, timecode, embed, filename }: PropsWithChildren<BasicControlsProps>): React.JSX.Element;
export {};
//# sourceMappingURL=basic-controls.d.ts.map