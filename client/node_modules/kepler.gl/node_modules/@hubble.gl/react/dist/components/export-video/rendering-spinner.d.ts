import React from 'react';
import { DeckAdapter } from '@hubble.gl/core';
type RenderingSpinnerProps = {
    rendering: boolean;
    saving: boolean;
    width: number;
    height: number;
    adapter: DeckAdapter;
    durationMs: number;
};
/**
 * @param props
 * @returns React Component that gives user feedback after they click the "Render" button
 */
export declare function RenderingSpinner({ rendering, saving, width, height, adapter, durationMs }: RenderingSpinnerProps): React.JSX.Element;
export {};
//# sourceMappingURL=rendering-spinner.d.ts.map