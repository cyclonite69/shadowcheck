import Keyframes, { KeyframeProps } from "./keyframes.js";
export type KeplerLayer = {
    id: string;
    config: {
        label: string;
        visConfig: object;
    };
};
export type KeplerLayerKeyframeProps<T> = KeyframeProps<T> & {
    layer?: KeplerLayer;
};
declare class KeplerLayerKeyframes<T extends object> extends Keyframes<T> {
    layer: KeplerLayer;
    constructor({ layer, timings, keyframes, easings }: KeplerLayerKeyframeProps<T>);
    set({ layer, timings, keyframes, easings, interpolators }: KeplerLayerKeyframeProps<T>): void;
}
export default KeplerLayerKeyframes;
//# sourceMappingURL=kepler-layer-keyframes.d.ts.map