import Keyframes, { KeyframeProps } from "./keyframes.js";
export type KeplerAnimationConfig = {
    domain: [number, number];
};
export type TripDataType = {
    currentTime: number;
};
export type KeplerTripKeyframeProps = Omit<KeyframeProps<TripDataType>, 'keyframes'> & {
    keyframes?: TripDataType[];
    animationConfig?: KeplerAnimationConfig;
};
declare class KeplerTripKeyframes extends Keyframes<TripDataType> {
    constructor({ animationConfig, timings, keyframes, easings, interpolators }: KeplerTripKeyframeProps);
    set({ animationConfig, timings, keyframes, easings, interpolators }: KeplerTripKeyframeProps): void;
    static _processParams({ animationConfig, timings, keyframes, easings, interpolators }: KeplerTripKeyframeProps): KeyframeProps<TripDataType>;
}
export default KeplerTripKeyframes;
//# sourceMappingURL=kepler-trip-keyframes.d.ts.map