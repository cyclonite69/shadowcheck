import Keyframes, { KeyframeProps } from "./keyframes.js";
export type DeckLayerKeyframeProps<T> = KeyframeProps<T> & {
    id: string;
};
export default class DeckLayerKeyframes<T extends object> extends Keyframes<T> {
    id: string;
    constructor({ id, features, timings, keyframes, easings }: DeckLayerKeyframeProps<T>);
}
//# sourceMappingURL=deck-layer-keyframes.d.ts.map