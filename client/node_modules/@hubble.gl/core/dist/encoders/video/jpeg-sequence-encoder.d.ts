import type { FrameEncoderSettings } from "../frame-encoder.js";
import FrameEncoder from "../frame-encoder.js";
import TARBuilder from "../tar/tar-builder.js";
declare class JPEGSequenceEncoder extends FrameEncoder {
    tarBuilder: TARBuilder | null;
    filemap: {
        [filename: string]: ArrayBuffer;
    };
    options: {
        quality: number;
        archive?: 'tar' | 'zip';
    };
    constructor(settings: FrameEncoderSettings);
    start(): void;
    add(canvas: HTMLCanvasElement): Promise<void>;
    save(): Promise<Blob>;
}
export default JPEGSequenceEncoder;
//# sourceMappingURL=jpeg-sequence-encoder.d.ts.map