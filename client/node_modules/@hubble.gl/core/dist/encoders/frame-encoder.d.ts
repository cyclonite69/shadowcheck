export type FrameEncoderSettings = Partial<EncoderSettings>;
interface EncoderSettings extends FormatConfigs {
    framerate: number;
}
export interface FormatConfigs {
    png: {
        archive?: 'tar' | 'zip';
    };
    jpeg: {
        archive?: 'tar' | 'zip';
        quality: number;
    };
    webm: {
        quality: number;
    };
    gif: {
        numWorkers?: number;
        sampleInterval: number;
        width: number;
        height: number;
        jpegQuality?: number;
    };
}
export default class FrameEncoder {
    extension: string;
    mimeType: string;
    quality?: number;
    framerate: number;
    constructor(settings?: FrameEncoderSettings);
    start(): void;
    add(canvas: HTMLCanvasElement): Promise<void>;
    save(): Promise<Blob | null>;
}
export {};
//# sourceMappingURL=frame-encoder.d.ts.map