import { Timeline } from '@luma.gl/engine';
import type Animation from "./animation.js";
import type { KeyframeProps } from "../keyframes.js";
export default class AnimationManager {
    timeline: Timeline;
    animations: {
        [id: string]: Animation;
    };
    constructor({ timeline, animations }: {
        timeline?: Timeline;
        animations?: Animation[];
    });
    attachAnimation(animation: Animation): void;
    setKeyframes(animationId: string, params: {
        [id: string]: KeyframeProps<object>;
    }): void;
    getKeyframes(animationId: string): {
        [id: string]: import("../keyframes").Keyframes<any> | {
            [id: string]: import("../keyframes").Keyframes<any>;
        };
    };
    getAnimation(animationId: string): Animation;
    draw(): void;
}
//# sourceMappingURL=animation-manager.d.ts.map