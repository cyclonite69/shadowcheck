import type { Timeline } from '@luma.gl/engine';
import type Keyframes from "../keyframes/keyframes.js";
type SetKeyframesBase = {
    [id: string]: Keyframes<any> | Timeline;
};
type SetKeyframesProps = SetKeyframesBase & {
    timeline?: Timeline;
};
type GetKeyframes = {
    [id: string]: Keyframes<any> | {
        [id: string]: Keyframes<any>;
    } | undefined;
};
export type AnimationConstructor = {
    id?: string;
};
export default class Animation {
    id: string;
    unattachedKeyframes: Keyframes<any>[];
    constructor({ id }: AnimationConstructor);
    attachKeyframes(timeline: Timeline): void;
    getKeyframes(): GetKeyframes;
    setKeyframes(props: SetKeyframesProps): void;
    animator(animation: this): void;
    draw(): void;
}
export {};
//# sourceMappingURL=animation.d.ts.map